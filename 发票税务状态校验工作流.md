# 发票税务状态校验工作流

将税务系统导出的 Excel 与飞书业务回票表（`tblNd9zgN7ALihwj`）比对，把未找到或状态异常的发票写回「税务系统状态」，并在群内回复结果。

## 触发

| 项 | 值 |
| --- | --- |
| 群 | `oc_546b26752d3d6763463f81344c7e77ef` |
| 文案 | 含「发票号校验」（富文本消息） |
| 附件 | Excel（`.xlsx` / `.xls`），sheet 名必须为 **发票基础信息** |
| Listener 路由 | `invoice-tax-status-check` → `http://n8n:5678/webhook/invoice-tax-check` |

操作：在固定群发送「发票号校验」+ 税务导出表附件。对话机器人需在群内。

## 比对规则

- **匹配键**：Excel「数电发票号码」↔ 回票表「发票号码」
- **回票多号**：单元格内用 `，` / `,` / `；` / `;` 分隔，先展开为「号码 → record_id」
- **通过条件**：`发票状态=正常` **且** `发票风险等级=正常`
- **回写**：只更新有问题的记录；正常记录不写
- **文案示例**：`未找到:2633…；状态异常(作废/正常):2633…`
- **群回复**：`校验完成：N 条有问题`（N = 有问题记录数）

## 部署

1. 确认 `.env` 中有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET`、`FEISHU_CHAT_APP_ID` / `FEISHU_CHAT_APP_SECRET`、`FEISHU_BASE_ID`
2. `feishu-listener/config.yaml` 已含路由（热加载，无需重启）
3. 将 [`files/invoice-tax-status-check.workflow.json`](files/invoice-tax-status-check.workflow.json) 导入 n8n，激活工作流
4. 确认对话应用能读群消息附件、主应用能读写业务回票表

## 凭证分工

| 用途 | 应用 |
| --- | --- |
| 下载 IM 附件、群内回复 | 对话应用 `FEISHU_CHAT_APP_*` |
| 拉取/更新多维表格 | 主应用 `FEISHU_APP_*` |

## 联调检查

1. 发「发票号校验」+ 含「发票基础信息」的 xlsx  
2. 有问题行「税务系统状态」被更新；正常行不变  
3. 群内回复 `校验完成：N 条有问题`  
4. 负例：无附件 / 错 sheet / 文案不含关键字 → 不应误伤 feishu-bot，或收到失败回复

## 本地单测

```bash
# listener 路由与附件规范化
cd feishu-listener && python -m unittest test_routes.py -v

# 比对算法
python scripts/test_invoice_tax_compare_logic.py -v
```

## 回滚

- `config.yaml` 中将 `invoice-tax-status-check` 的 `enabled` 设为 `false`
- n8n 停用「发票税务状态校验」工作流
