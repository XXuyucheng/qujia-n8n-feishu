# 发票税务状态校验工作流

将税务系统导出的 Excel 与飞书业务回票表（`tblNd9zgN7ALihwj`）比对，把未找到或状态异常的发票写回「税务系统状态」，并在群内回复结果。

## 触发

| 项 | 值 |
| --- | --- |
| 群 | `oc_546b26752d3d6763463f81344c7e77ef` |
| 机器人应用 | 主应用 `cli_a94bb777f63a5bce`（`FEISHU_APP_ID`，连接 n8n） |
| 文案 | 单独发一条含「发票号校验」的文本（触发口令） |
| Excel | **另发一条文件消息**（飞书不支持富文本+文件同条）；工作流从群内**最近一条** `.xlsx`/`.xls` 文件消息拉取 |
| sheet | 必须为 **发票基础信息** |
| Listener 路由 | `invoice-tax-status-check` → `http://n8n:5678/webhook/invoice-tax-check` |

操作顺序：

1. 先在固定群发送税务导出 Excel（文件消息）  
2. 再发送文本「发票号校验」  

listener 只负责触发与传入 `chat_id` / `message_id`；**不**从触发事件解析附件。**主应用机器人**需在群内，并具备拉取群消息历史、下载消息资源权限。

## 比对规则

- **匹配键**：Excel「数电发票号码」↔ 回票表「发票号码」
- **回票多号**：单元格内用 `，` / `,` / `；` / `;` 分隔，先展开为「号码 → record_id」
- **通过条件**：`发票状态=正常` **且** `发票风险等级=正常`
- **回写**：只更新有问题的记录；正常记录不写
- **文案示例**：`未找到:2633…；状态异常(作废/正常):2633…`
- **群回复**：`校验完成：N 条有问题`（N = 有问题记录数）

## 工作流节点（优化后）

1. Webhook → 解析事件 → **获取Token（仅一次）**
2. 拉取群最近消息 → 定位最近 Excel → 下载 → Extract → 构建 taxMap
3. **查询回票表**（HTTP，按 Excel 中出现的「购买方名称」过滤回票表「购方名称」）→ **整理回票Map**（Code，含翻页）→ **比对生成更新**（Code）
4. 有问题则 **批量回写**；最后群回复

后续 HTTP 一律引用 `$('获取Token').item.json.tenant_access_token`，不再重复取 token。

查询过滤：从 sheet「发票基础信息」收集去重后的 `购买方名称`，对业务回票表 `购方名称` 做 `OR + is` 条件（最多 50 个购买方）。

## 部署

1. 确认 `.env` 中有 `FEISHU_APP_ID` / `FEISHU_APP_SECRET`、`FEISHU_BASE_ID`（IM 下载与 bitable 均用主应用）
2. 飞书开放平台 → 主应用事件订阅：开启 **长连接**，并订阅 `im.message.receive_v1`
3. `feishu-listener/config.yaml` 已含路由（配置热加载；**代码变更需重建** `feishu-listener`）
4. 工作流文件：[`files/invoice-tax-status-check.workflow.json`](files/invoice-tax-status-check.workflow.json)（id=`GPCcJKuLDQqYe3sd`）
5. 在 n8n UI **重新激活**「发票税务状态校验」（CLI 导入后会停用）
6. 确认主应用机器人在目标群内，且具备：获取群消息、下载消息资源、发送消息、读写业务回票表

## 凭证分工

| 用途 | 应用 |
| --- | --- |
| 接收群消息、下载 IM 附件、群内回复 | 主应用 `FEISHU_APP_*`（`cli_a94bb777f63a5bce`） |
| 拉取/更新多维表格 | 主应用 `FEISHU_APP_*`（同上） |

> 对话应用 `FEISHU_CHAT_APP_*`（`cli_a940966ee8385bd7`）仍只服务 feishu-bot Skill，与本校验无关。

## 联调检查

1. 先发含「发票基础信息」的 xlsx，再发「发票号校验」  
2. 有问题行「税务系统状态」被更新；正常行不变  
3. 群内回复 `校验完成：N 条有问题`  
4. 负例：群内无近期 Excel / 错 sheet / 文案不含关键字 → 失败回复或不应进本路由

## 本地单测

```bash
# listener 路由与附件规范化
cd feishu-listener && python -m unittest test_routes.py -v

# 比对算法
python scripts/test_invoice_tax_compare_logic.py -v
```

## 回滚

- `config.yaml` 中将 `invoice-tax-status-check` 的 `enabled` 设为 `false`
- n8n 停用「发票税务状态校验」工作流
