# 出团计划单模板说明与占位符约定

> 飞书云文档模板需人工创建一次；本文件记录正文结构与 `{{占位符}}` / Sheet 列映射，供 `feishu-files-generation` 与 n8n 对齐。

## 一、正文结构（云文档文本区）

在飞书 docx 模板中按下列结构书写，占位符必须落在**同一个 text_run** 内（勿拆成「{{」+「字段」+「}}」）。

```text
## 团队基本信息

订单编号：{{订单编号}}
客户领队：{{客户领队}}
执行时间：{{执行时间}}
导游：{{导游}}
客户名称：{{客户名称}}
人数：{{人数}}
车型：{{车型}}
车牌：{{车牌}}
司机：{{司机}}
活动：{{活动}}
教练：{{教练}}
物料：{{物料}}

---

注意事项：请各点位之间相互配合相互尊重 需要其他点位配合的内容信息++在工作群交流共享++ 百忙之中抽空多关注工作群的消息 如果有机密消息++可私发策划++ 策划非工作时间如不能及时回复消息还请见谅

---

## 行程内容

{{活动行程}}

---

## 预定结算
```

其下插入**内嵌电子表格（Sheet 块）**，不要用原生文档表格。

## 二、预定结算 Sheet 约定

| 列 | 含义 | 是否由服务写入 |
| --- | --- | --- |
| A | 物品名称 | 是（总价行写「总价」标签） |
| B | 内容/描述 | 是 |
| C | 数量 | 是 |
| D | 单价 | 是 |
| E | 小计 | **是**（服务按行写入公式，如 `=IF(C2="","",C2*D2)`） |
| F | 结算方式 | 是（可空） |
| G | 联系人 | 是（可空） |

写入流程（`feishu-files-generation`）：

1. 仅按实际明细行数 `N` 写入 A–D / F–G（上限 `max_rows`，默认 50）
2. 写入 E2:E_{N+1} 小计公式，并在下一行写入总价 `=SUM(E2:E_{end})`
3. 删除总价行之后到模板预留末尾的空行（`trim_unused_rows` + `template_data_rows`），得到紧凑表

模板侧建议：

- 第 1 行：表头
- 可预留约 20 行数据区 + 末行总价作兜底（复制后会被服务覆盖并裁剪）
- 不必再为「防覆盖总价」强行留很多空行；长期可缩成「表头 + 少量空行 + 总价」

`config.yaml` 关键字段示例：`trim_unused_rows`、`template_data_rows`、`formula.*`。

## 三、飞书 token 配置

创建模板并嵌入 Sheet 后，把 token 写入环境变量 / [`feishu-files-generation/config.yaml`](feishu-files-generation/config.yaml)：

| 变量 | 用途 |
| --- | --- |
| `FEISHU_DEPARTURE_TEMPLATE_TOKEN` | 出团计划单模板 docx token |
| `FEISHU_DEPARTURE_OUTPUT_FOLDER` | 出团计划单输出文件夹 token（可与合同共用） |

`config.yaml` 中模板键：`出团计划单`。n8n 调用时传 `signing_unit: "出团计划单"`。

## 四、校验清单

1. `POST /api/probe` 能扫到文本占位符（订单编号、活动行程等）
2. 复制模板后 blocks 中存在 `block_type=30`，且 `sheet.token` 可拆为 `SpreadsheetToken_SheetID`
3. 生成后预定结算表为「表头 + N 行明细 + 1 行总价」，无大片空行
4. 改数量/单价后，小计与总价公式自动重算
