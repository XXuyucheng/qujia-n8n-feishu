# 出团计划单模板说明与占位符约定

> 飞书云文档模板需人工创建一次；本文件记录正文结构与 `{{占位符}}` / 文末结算表约定，供 `feishu-files-generation` 与 n8n 对齐。

## 一、正文结构（云文档文本区）

在飞书 docx 模板中按下列结构书写，占位符必须落在**同一个 text_run** 内（勿拆成「{{」+「字段」+「}}」）。

```text
## 团队基本信息

订单编号：{{订单编号}}
客户对接人：{{客户对接人}}
执行时间：{{执行时间}}
工作人员：{{工作人员}}
客户名称：{{客户名称}}
人数：{{人数}}
车型：{{车型}}
活动：{{活动}}
教练：{{教练}}
活动项目：{{活动项目}}
策划师：{{策划师}}

---

## 行程内容

{{行程内容}}
```

**不要**在模板里预埋「预定结算」内嵌电子表格。结算表由服务在文末创建（H2 + Sheet）。

若模板里仍留有旧的内嵌 Sheet，请手动删除，否则生成文档会叠两张表。

## 二、文末预定结算表（服务追加）

由 `config.yaml` → `templates.出团计划单.detail_sheets.settlement` 控制：

| 列 | 字段 | 默认宽度 | 写入方式 |
| --- | --- | --- | --- |
| A | 物品名称 | 150 | 值（`名称` 别名） |
| B | 内容 | 200 | 值（`描述` 别名） |
| C | 数量 | 40 | 数值 |
| D | 单价 | 50 | 数值 |
| E | 小计 | 60 | 公式 `=C*D` |
| F | 预定状态 | 70 | 值（可空） |
| G | 结算方式 | 70 | 值（可空） |
| H | 联系人 | 70 | 值（可空） |
| I | 预定信息 | 250 | 值（可空） |

- 数据来源：请求 `sheet_rows`，否则 `fields.报价明细条目`
- 末行：A=`总价`，E=`=SUM(E2:E_n)`
- 列宽 / 单元格对齐 / 字号见 `detail_sheets.style` 与各列 `width`
- H2 标题对齐：`detail_sheets.style.title_align`（`left` | `center` | `right`）；也可在 `settlement.title_align` 单独覆盖

## 三、飞书 token 配置

| 变量 / 配置 | 用途 |
| --- | --- |
| `FEISHU_DEPARTURE_TEMPLATE_TOKEN` | 出团计划单模板 docx token |
| `output_folders.出团计划单` / `FEISHU_DEPARTURE_OUTPUT_FOLDER` | 输出文件夹 |

`config.yaml` 模板键：`出团计划单`。n8n 传 `signing_unit: "出团计划单"`。

## 四、校验清单

1. `POST /api/probe` 能扫到正文占位符
2. 模板中**无**预埋结算 Sheet（或已删除）
3. 生成后文末有「预定结算」表：表头 + N 行明细 + 1 行总价，小计/总价为公式
4. 改数量/单价后小计与总价自动重算
