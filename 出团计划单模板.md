# 出团计划单模板说明与占位符约定

> 飞书云文档模板需人工创建一次；本文件记录正文结构与 `{{占位符}}` / Sheet 列映射，供 `contract-generator` 与 n8n 对齐。

## 一、正文结构（云文档文本区）

在飞书 docx 模板中按下列结构书写，占位符必须落在**同一个 text_run** 内（勿拆成「{{」+「字段」+「}}」）。

```text
## 团队基本信息

订单编号：{{订单编号}}
客户领队：{{客户领队}}
执行时间：{{执行时间}}
导游：{{导游}}
客户名称：{{客户名称}}
人数：{{人数}}
车型：{{车型}}
车牌：{{车牌}}
司机：{{司机}}
活动：{{活动}}
教练：{{教练}}
物料：{{物料}}

---

注意事项：请各点位之间相互配合相互尊重 需要其他点位配合的内容信息++在工作群交流共享++ 百忙之中抽空多关注工作群的消息 如果有机密消息++可私发策划++ 策划非工作时间如不能及时回复消息还请见谅

---

## 行程内容

{{活动行程}}

---

## 预定结算
```

其下插入**内嵌电子表格（Sheet 块）**，不要用原生文档表格。

## 二、预定结算 Sheet 约定

| 列 | 含义 | 是否由服务写入 |
| --- | --- | --- |
| A | 物品名称 | 是 |
| B | 内容 | 是 |
| C | 数量 | 是 |
| D | 单价 | 是 |
| E | 小计 | **否**（模板预置公式，如 `=IF(C2="","",C2*D2)`） |
| F | 结算方式 | 是（可空） |
| G | 联系人 | 是（可空） |

- 第 1 行：表头
- 第 2～21 行：数据区（`max_rows: 20`）
- 末行或单独单元格：总价 `=SUM(E2:E21)`，**服务不覆盖**

## 三、飞书 token 配置

创建模板并嵌入 Sheet、写好公式后，把 token 写入环境变量 / [`contract-generator/config.yaml`](contract-generator/config.yaml)：

| 变量 | 用途 |
| --- | --- |
| `FEISHU_DEPARTURE_TEMPLATE_TOKEN` | 出团计划单模板 docx token |
| `FEISHU_DEPARTURE_OUTPUT_FOLDER` | 生成文档输出文件夹 token（可与合同共用） |

`config.yaml` 中模板键：`出团计划单`。n8n 调用时传 `signing_unit: "出团计划单"`。

## 四、校验清单

1. `POST /api/probe` 能扫到文本占位符（订单编号、活动行程等）
2. 复制模板后 blocks 中存在 `block_type=30`，且 `sheet.token` 可拆为 `SpreadsheetToken_SheetID`
3. 仅改数量列后，小计/总价公式自动重算
